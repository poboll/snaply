# 常见问题 (FAQ)

本文档收集了 Snaply 使用过程中的常见问题及解答。

## 📚 目录

- [安装相关](#安装相关)
- [配置相关](#配置相关)
- [AI 功能](#ai-功能)
- [上传相关](#上传相关)
- [存储相关](#存储相关)
- [部署相关](#部署相关)
- [性能优化](#性能优化)

---

## 安装相关

### Q1: Snaply 需要什么运行环境？

**A:**
- **Node.js**: >= 18.0.0（推荐 20.x LTS）
- **pnpm**: >= 8.0.0（推荐 9.x）
- **操作系统**: macOS / Linux / Windows
- **内存**: 至少 2GB RAM
- **磁盘**: 至少 1GB 可用空间

---

### Q2: 为什么推荐使用 pnpm 而不是 npm/yarn？

**A:**
- ⚡ **速度更快**: pnpm 使用硬链接，安装速度比 npm 快 2-3 倍
- 💾 **节省空间**: 相同的包只存储一次，节省 70% 磁盘空间
- 🔒 **更严格**: 严格的依赖管理，避免幽灵依赖
- 📦 **支持 workspace**: Snaply 使用 monorepo 架构

---

### Q3: Windows 上安装依赖失败怎么办？

**A:**
```powershell
# 1. 以管理员身份运行 PowerShell
# 2. 启用脚本执行
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 3. 重新安装依赖
pnpm install

# 4. 如果仍有问题，清理缓存
pnpm store prune
rmdir -Recurse -Force node_modules
pnpm install
```

---

## 配置相关

### Q4: 如何修改上传文件大小限制？

**A:** 有两种方式：

**方式 1: 通过界面配置**
1. 打开 Snaply
2. 点击 "Config" 按钮
3. 切换到 "Advanced" 标签
4. 修改 "Max File Size (MB)"

**方式 2: 直接编辑配置文件**
```bash
# 编辑 server/data/config.json
{
  "advanced": {
    "maxFileSize": 20  // 修改为 20MB
  }
}
```

然后重启后端服务。

---

### Q5: 如何配置自定义域名/CDN？

**A:**
1. 打开 Snaply
2. 进入 "Config" → "常规设置"
3. 在 "公开 URL" 字段填写：
   ```
   https://cdn.example.com
   ```
4. 保存配置

配置后，所有图片链接都会使用该域名前缀：
```
https://cdn.example.com/uploads/abc123.jpg
```

---

### Q6: 配置文件在哪里？

**A:** Snaply 的配置文件位置：

| 文件 | 路径 | 说明 |
|-----|------|------|
| **配置** | `server/data/config.json` | 系统配置（存储、AI 等） |
| **图片数据库** | `server/data/images.json` | 图片元数据 |
| **环境变量** | `server/.env` | 环境变量（可选） |

⚠️ **注意**: 这些文件已被 `.gitignore` 忽略，不会被提交到 Git。

---

## AI 功能

### Q7: AI 自动打标免费吗？

**A:** 取决于您选择的提供商：

| 提供商 | 费用 | 说明 |
|-------|------|------|
| **Ollama** | ✅ 完全免费 | 本地部署，需要一定硬件配置 |
| **硅基流动** | ⚡ 新用户免费 | 赠送免费额度，用完后按量计费 |
| **通义千问** | 💰 按量计费 | 提供免费试用额度 |
| **Gemini** | 💰 按量计费 | Google 提供免费层 |
| **智谱 AI** | 💰 按量计费 | 新用户有免费额度 |

**推荐方案**:
- 🏠 个人使用：Ollama（免费）
- 🚀 快速体验：硅基流动（免费额度多）
- 🏢 生产环境：通义千问 / Gemini（稳定性高）

---

### Q8: Ollama 需要什么配置？

**A:** Ollama 的最低配置要求：

**硬件要求**:
- **CPU**: 支持 AVX2 指令集
- **内存**: 至少 8GB RAM（推荐 16GB）
- **磁盘**: 至少 10GB 可用空间
- **系统**: macOS / Linux / Windows (WSL2)

**快速测试**:
```bash
# 安装 Ollama
# macOS
brew install ollama

# 启动 Ollama 服务
ollama serve

# 下载小模型测试
ollama pull moondream

# 测试运行
ollama run moondream
```

如果运行卡顿，可以：
- 换用更小的模型（如 `moondream`）
- 增加系统内存
- 使用云 API 替代

---

### Q9: AI 打标失败怎么办？

**A:** 常见原因及解决方案：

**1. API Key 无效**
```bash
# 检查配置
cat server/data/config.json | grep apiKey

# 重新配置正确的 Key
```

**2. 网络问题**
```bash
# 测试网络连接
curl -I https://api.siliconflow.cn

# 使用代理
export HTTP_PROXY=http://127.0.0.1:7890
export HTTPS_PROXY=http://127.0.0.1:7890
```

**3. 图片太大**
```bash
# Snaply 会自动压缩 >2MB 的图片
# 如果仍然失败，检查 Sharp 是否正确安装
npm list sharp
```

**4. 模型名称错误**
- 确保模型名称与提供商文档一致
- 参考 [AI 配置文档](AI-Setup)

---

## 上传相关

### Q10: 支持哪些图片格式？

**A:** Snaply 支持以下格式：

| 格式 | 扩展名 | 说明 |
|-----|--------|------|
| **JPEG** | `.jpg`, `.jpeg` | 最常用格式 |
| **PNG** | `.png` | 支持透明背景 |
| **GIF** | `.gif` | 动图支持 |
| **WebP** | `.webp` | 现代化格式（推荐） |

未来计划支持：SVG、AVIF、HEIC

---

### Q11: 为什么上传的图片没有自动打标？

**A:** 检查以下几点：

1. **确认 AI 已启用**
   ```json
   // server/data/config.json
   {
     "ai": {
       "enabled": true  // 必须为 true
     }
   }
   ```

2. **确认 AI 提供商配置正确**
   - Ollama: 确保 `ollama serve` 正在运行
   - 云 API: 确保 API Key 有效

3. **测试 AI 连接**
   - 打开 Snaply → Config → AI
   - 点击 "🧪 测试 AI 连接"
   - 查看测试结果

4. **查看后端日志**
   ```bash
   # 开发环境
   pnpm dev:server

   # 查看日志输出
   [AI] 开始异步自动打标 - Provider: ollama
   ```

---

### Q12: 批量上传有限制吗？

**A:** 默认配置：

- **最大文件数**: 无限制（浏览器限制约 100 个）
- **单文件大小**: 默认 10MB（可配置）
- **总大小**: 受服务器内存限制

建议：
- 🖥️ **桌面端**: 一次上传 20-50 张
- 📱 **移动端**: 一次上传 5-10 张
- 🔄 **大批量**: 分批上传，避免超时

---

## 存储相关

### Q13: 本地存储和 S3/MinIO 如何选择？

**A:** 对比分析：

| 特性 | 本地存储 | S3 | MinIO |
|-----|---------|-----|-------|
| **成本** | 免费 | 按量计费 | 硬件成本 |
| **速度** | 最快 | 取决于网络 | 局域网快 |
| **扩展性** | ❌ 差 | ✅ 优秀 | ✅ 良好 |
| **可靠性** | 单点故障 | 99.9999999% | 取决于配置 |
| **CDN** | 需自建 | 原生支持 | 需自建 |
| **适用场景** | 个人使用 | 生产环境 | 私有云 |

**推荐**:
- 🏠 **个人图床**: 本地存储
- 🚀 **生产环境**: Amazon S3
- 🔒 **数据敏感**: 自建 MinIO

---

### Q14: 如何迁移存储？

**A:** Snaply 暂未提供自动迁移工具，手动迁移步骤：

**从本地迁移到 S3**:

```bash
# 1. 配置 S3 存储
# 在 Config 界面切换到 S3

# 2. 上传历史图片到 S3
# 使用 AWS CLI
aws s3 sync ./uploads s3://my-bucket/uploads/

# 3. 更新数据库中的 URL
# 编辑 server/data/images.json
# 将 "/uploads/xxx.jpg" 替换为 "https://cdn.example.com/xxx.jpg"

# 4. 测试访问
curl -I https://cdn.example.com/uploads/xxx.jpg
```

---

### Q15: MinIO 配置一直失败？

**A:** 常见问题：

**1. Access Key 格式错误**
```
错误: InvalidAccessKeyId
解决: 桶名必须是小写，不能包含特殊字符
```

**2. 端点 URL 错误**
```bash
# 错误
https://minio.example.com:9000

# 正确（如果是 HTTP）
http://minio.example.com:9000

# 正确（如果是 HTTPS）
https://minio.example.com
```

**3. 桶策略问题**
```bash
# 使用 MinIO Client 设置公开读
mc anonymous set download my-bucket
```

---

## 部署相关

### Q16: Docker 部署如何持久化数据？

**A:** 使用 Docker Volume 挂载：

```yaml
# docker-compose.yml
services:
  snaply:
    volumes:
      - ./uploads:/app/uploads      # 上传的图片
      - ./data:/app/data            # 配置和数据库
```

**备份命令**:
```bash
# 备份数据
tar -czf snaply-backup-$(date +%Y%m%d).tar.gz ./data ./uploads

# 恢复数据
tar -xzf snaply-backup-20260120.tar.gz
```

---

### Q17: 如何使用 Nginx 反向代理？

**A:** 完整配置示例：

```nginx
server {
    listen 80;
    server_name img.example.com;

    # 前端静态文件
    location / {
        proxy_pass http://localhost:5173;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # API 接口
    location /api/ {
        proxy_pass http://localhost:3000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;

        # 上传大文件
        client_max_body_size 50M;
        proxy_read_timeout 300s;
    }

    # 上传的图片
    location /uploads/ {
        proxy_pass http://localhost:3000/uploads/;
        proxy_set_header Host $host;

        # 缓存配置
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Gzip 压缩
    gzip on;
    gzip_types image/jpeg image/png image/gif;
    gzip_min_length 1000;
}
```

---

### Q18: Railway 部署后图片丢失？

**A:** Railway 的文件系统是**临时的**，重启后会清空。

**解决方案**:

1. **使用 S3/MinIO 存储**（推荐）
2. **挂载持久化存储**（Railway 支持但需付费）
3. **定期备份到外部存储**

---

## 性能优化

### Q19: 如何提升上传速度？

**A:** 优化建议：

**1. 启用 WebP 转换**
- 文件更小（减少 30-50%）
- 配置: Config → Advanced → 启用 WebP

**2. 使用 CDN**
- 配置公开 URL 为 CDN 域名
- 推荐: Cloudflare、阿里云 CDN

**3. 调整并发数**
```typescript
// src/api.ts
// 修改并发上传数量
const MAX_CONCURRENT = 5  // 默认 3
```

**4. 服务端优化**
```bash
# 增加 Node.js 内存
NODE_OPTIONS="--max-old-space-size=4096" pnpm start
```

---

### Q20: 图片很多时画廊加载慢？

**A:** 优化方案：

**1. 启用分页**（TODO: 未来版本会支持）

**2. 使用标签过滤**
- 只显示需要的图片
- 减少渲染数量

**3. 懒加载**
```vue
<!-- GalleryView.vue 已支持 -->
<img loading="lazy" :src="image.url" />
```

**4. 使用缩略图**（TODO: 未来版本会支持）

---

## 其他问题

### Q21: Snaply 是开源的吗？

**A:** 是的！Snaply 采用 [MIT License](https://github.com/poboll/snaply/blob/main/LICENSE) 开源。

您可以：
- ✅ 自由使用（个人/商业）
- ✅ 修改源码
- ✅ 分发
- ❌ 需要保留版权声明

---

### Q22: 如何参与贡献？

**A:** 欢迎贡献！请阅读 [贡献指南](Contributing)

**贡献方式**:
- 🐛 报告 Bug
- 💡 提出功能建议
- 📝 改进文档
- 💻 提交 Pull Request
- 🌍 帮助翻译

---

### Q23: 有没有演示站点？

**A:** 暂时没有官方演示站点。您可以：

1. **本地体验**
   ```bash
   git clone https://github.com/poboll/snaply.git
   cd snaply
   pnpm install
   pnpm dev
   ```

2. **Railway 一键部署**（5 分钟内搭建）

---

## 📞 更多帮助

如果您的问题不在列表中：

1. 📖 查看 [完整文档](Home)
2. 🔍 搜索 [Issues](https://github.com/poboll/snaply/issues)
3. 💬 提交新 Issue
4. 💬 加入讨论（TODO: Discord/微信群）

---

**最后更新**: 2026-01-20
